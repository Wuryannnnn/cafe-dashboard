# 测试时关掉 websocket: MockServletContext 起不来 ServerContainer
websocket.enabled=false

# 每次 SpringBoot context 重启时, H2 通过 INIT 子句先把所有对象 drop, 这样 data-local.sql 重跑不会 PK 冲突
spring.datasource.url=jdbc:h2:mem:sell;DB_CLOSE_DELAY=-1;MODE=MySQL;INIT=DROP ALL OBJECTS
spring.jpa.hibernate.ddl-auto=create
